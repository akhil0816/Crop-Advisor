events {}

http {
    server {
        listen 80;

        # Frontend
        location / {
            proxy_pass http://frontend:5173;
        }

        # Backend
        location /api {
            proxy_pass http://backend:5000;
        }
    }
}