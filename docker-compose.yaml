version: "3.9"

services:

  backend:
    build: ./my_backend
    container_name: crop_backend
    ports:
      - "5001:5000"

  frontend:
    build: ./frontend
    container_name: crop_frontend
    ports:
      - "80:80"
    depends_on:
      - backend
